# 借入金台帳の再設計 ＋ 対話型AI起票

計画: `~/.claude/plans/crispy-painting-candle.md`
ブランチ: `feat/loan-ledger-redesign`

## P0: 台帳の作り替え

- [x] 1. `supabase/migrations/040_loan_ledger.sql`
  - [x] `loans` のヘッダ化（direction / counterparty_kind / business_partner_id / repayment_terms）
  - [x] `loan_entries`（増減明細）＋ RLS
  - [x] `loan_entry_receipts`（証憑の多対多）＋ RLS
  - [x] `statutory_interest_rates`（認定利息の利率マスタ）
  - [x] 勘定科目 seed（役員借入金 / 役員貸付金）
  - [x] `journal_entries.source` に 'loan' を追加
  - [x] 既存データの自動移行（差額は adjust 明細）
- [x] 2. 型定義（`src/types/index.ts` / `src/types/database.ts`）
- [x] 3. `src/lib/loan-ledger.ts`（純関数）＋ `loan-ledger.test.ts`
- [x] 4. `src/actions/loans.ts` 全面改修 ＋ **認可ガード追加**（既存のIDOR是正）
- [x] 5. UI: 増減明細・残高推移・区分別フォーム出し分け・6章の文字視認性

## P1: 認定利息アラート ＋ AI

- [x] 6. 役員貸付金サマリー・決算日カウントダウン・認定利息試算
- [x] 7. `src/lib/gemini.ts`（共通ラッパー）
- [x] 8. `src/actions/loan-ai.ts`
  - [x] 4-3 自然言語からの起票
  - [x] 4-4 証憑PDFからの一括起票
  - [x] 4-5 曖昧な取引の判別（確認ループ）＋UI接続
- [x] 9. AI確認パネルUI

## P2 / P3（2026-09-08 追加実施）

- [x] 3-7 返済予定表（元金均等・元利均等の自動生成、1クリックで明細へ消し込み）
- [x] 3-5 / 4-6 台帳と仕訳の照合（差額と原因候補の提示）
- [x] 3-6 勘定科目内訳明細書「借入金及び支払利子の内訳書」の出力
- [x] 4-7 質問応答（残高はサーバー側で確定させ、根拠の明細を必ず添える）

## P2/P3 の画面確認（2026-09-09 実施・完了）

- [x] A 返済予定表の生成（元金均等/元利均等、元金合計が借入額と一致、月末指定）
- [x] B 予定の消し込み → 返済明細が作られ、利息が残高を増やさない
- [x] C 台帳と仕訳の照合（未仕訳時に差額と原因を提示 → 仕訳化で一致）
- [x] D 内訳明細書の出力（期末現在高・支払利子額・年度切替）
- [x] 検証データの削除

### この確認で見つけて直した不具合
- 返済時の利息が借入金残高を増やしていた（会計上の誤り）
- 支払利息が集計から漏れ、内訳明細書の支払利子額が常に0になっていた
- 予定の消し込み後に増減明細と残高が画面更新されない
- 明細の二重登録（連打で通ってしまう）
- ダークモードの明度差が不足（背景vsカード 1.15:1）
- 入力欄の幅の配分、素の日付入力の混在、表の見出し欠落
- 内訳明細書で事業年度を選べない（前期分が作れない）

## 3-4 役員貸付金・認定利息の画面確認（2026-09-10 実施・完了）

- [x] 段階1 期中の貸付 → 警告色・決算日カウントダウン・試算額
      1,000,000 × 1.3%（2026年の貸付）× 335日 = 11,932
- [x] 段階2 期末をまたぐ → 「認定利息の計上が必要」に切替。**貸付年ごとに利率と期間が分かれる**
      2025年分 2,000,000 × 0.9% × 364日（期首から）= 17,951
      2026年分 1,000,000 × 1.3% × 334日（実行日から）= 11,896  合計 29,847
- [x] 段階3 同一相手先の差引（貸付3,000,000 − 借入500,000 = 2,500,000 が貸付側）
      マイナス表記は使わず、正の値＋どちら側に残るかで表示
- [x] 検証データの削除

### この確認で見つけて直した不具合
- 認定利息の試算期間が「今日から決算日まで」で、経過期間が抜けて**金額が過小に出ていた**
  （5/1の貸付を9/10に見ると133日分が落ち、実際の6割程度になる）
- 「うち役員借入金」と「うち役員貸付金」が1文字違いで、同じ欄が2つあるように見えていた

## 検証

- [x] `npm test` 全通過（99件）
- [x] `npm run build` 通過
- [x] 画面操作確認 ステップ1〜4（2026-09-08 実施）
  - [x] 1. 台帳を開く。サマリーカード4枚・空状態
  - [x] 2. 区分の出し分け（役員→年利欄が消える／プレースホルダ切替／返済条件の既定値）
  - [x] 3. 借入65,000＋立替17,595−返済30,000 → 残高 52,595 が自動算出される
  - [x] 4. 立替の仕訳 = 借方 消耗品費 / 貸方 役員借入金。**現金・預金が動かないことをDBで確認**
        借入の仕訳 = 借方 普通預金 / 貸方 役員借入金（対比も確認）
- [ ] 5. AI起票の確認（**保留**: Gemini APIの前払いクレジット切れのため実施できず。
      クレジット追加後に「登録するまで台帳が変わらないこと」を確認する）
- [x] 検証データの削除（loans 1件・loan_entries 3件・journal_entries 2件）
- [x] 認可ガードを全アクションに追加（既存のIDOR是正）／実データでの越境テストは未実施

## Review

### 実施したこと
- 借入金台帳を「1レコード＝1相手先＋増減明細」に作り替えた。残高はDBに持たず `loan_entries` から常に算出する
- 立替（現金が動かず債務が増える）を区分として追加し、`借方 費用 / 貸方 役員借入金` で仕訳化できるようにした
- 証憑を明細行に多対多で紐付け、証憑内の行番号まで記録できるようにした
- 役員貸付金を `direction='lend'` で同じ構造に載せ、認定利息アラート（決算日カウントダウン → 期末跨ぎで「計上が必要」）を実装
- 対話型AI起票（自然言語 / 通帳PDF一括 / 役員送金の判別）を追加。AIは下書きのみ作り、DBへの反映は人間の操作を必須にした
- **既存の欠陥を是正**: `src/actions/loans.ts` はサービスロールでRLSを迂回しながら認可ガードを一度も呼んでいなかった（IDOR相当）。全アクションに `assertClientAccess` / `resolveClientIdForRecord` を追加

### 設計上の判断（計画から変えた点）
- 増減の符号は `direction × entry_type` ではなく **`entry_type` のみ**で決まる設計にした。残高は各台帳の方向における正の値で表すため（帳簿にマイナス表記を使わない方針に一致）。`direction` は残高の意味づけ（債務か債権か）だけを決める
- `principal` / `current_balance` / `loan_type` を DROP せず非推奨コメントに留めた。マイグレーション適用とコードデプロイが同時でないため、旧コードが動き続けられる状態を保つ
- 仕訳の借方貸方ルールを `src/lib/loan-ledger.ts` の純関数 `buildJournalLines` に切り出し、単体テストの対象にした

### 残課題
- **画面操作での動作確認が未実施**（ブラウザ起動の権限が必要）
- 認定利息の利率は国税庁タックスアンサー No.2606 の公表値で確定（2010〜2026）。
  2026年（令和8年）は当初 平均貸付割合0.8%＋0.5%＝1.3% の推定値だったが、
  同ページが「令和8年4月1日現在法令等」に更新され 1.3% と明記されたため確定（2026-09-11 確認・migration 050）。
  全17年分を公表値と突き合わせ、すべて一致。令和9年以降は未公表。
  未登録の年は試算を出さず「利率が未登録」と表示する作りのため、推測値は入れない
- **利率は「貸付けを行った暦年」で固定される**（会計年度でも当年でもない）。
  041 でマスタを loan_year 軸に修正し、貸付トランシェ単位で利率を保持するようにした
- P2以降（仕訳の逆方向反映・整合性チェック・内訳明細書出力・質問応答）は後続PR
- 既存7ファイルの `getGeminiClient` コピペは未集約（新規分のみ `src/lib/gemini.ts` を使用）
