-- ============================================================================
-- KaikeiPro 請求書明細に「取引年月日」を追加
-- 適格請求書（インボイス）の記載要件である「課税資産の譲渡等を行った年月日」を
-- 明細ごとに保持する。未設定の場合は請求書の発行日を取引日とみなす。
-- ============================================================================

ALTER TABLE invoice_items
    ADD COLUMN IF NOT EXISTS transaction_date date;
